---
import { SITE } from '~/config.mjs';

import { getCanonical, getPermalink } from '~/utils/permalinks';
import Layout from '~/layouts/PageLayout.astro';
import Note from '~/components/widgets/EditionsNote.astro';
import Editions from '~/components/widgets/Editions.astro';

const meta = {
  title: `Shop`,
  description: `Explore ${SITE.name} custom PC builds and configurator options.`,
  canonical: getCanonical(getPermalink('/download/')),
};
---

<Layout {meta}>
  <Note />
  <Editions />
</Layout>
