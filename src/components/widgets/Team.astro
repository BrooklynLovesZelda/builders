---
import Image from '~/components/common/Image.astro';
import { Icon } from 'astro-icon/components';

const items = [
  {
    name: 'Brooklyn',
    description: 'Founder, lead builder',
    avatar: '~/assets/images/logo.svg',
    github_url: 'mailto:hello@builders.bybrooklyn.dev',
  },
  {
    name: 'Build Tech',
    description: 'Assembly, testing, and QA',
    avatar: '~/assets/images/logo.svg',
    github_url: 'mailto:hello@builders.bybrooklyn.dev',
  },
  {
    name: 'Parts Specialist',
    description: 'Sourcing and compatibility checks',
    avatar: '~/assets/images/logo.svg',
    github_url: 'mailto:hello@builders.bybrooklyn.dev',
  },
  {
    name: 'Support',
    description: 'Order updates and build support',
    avatar: '~/assets/images/logo.svg',
    github_url: 'mailto:hello@builders.bybrooklyn.dev',
  },
];
---

<section class="py-28 overflow-hidden" id="team">
  <div class="py-8 px-4 mx-auto max-w-(--breakpoint-xl) text-center lg:py-16 lg:px-6">
    <div class="mx-auto mb-8 max-w-(--breakpoint-sm) lg:mb-16">
      <h2 class="mb-4 text-4xl tracking-tight font-extrabold text-gray-900 dark:text-white">
        Meet the Builders team
      </h2>
      <p class="font-light text-gray-500 sm:text-xl dark:text-gray-400">
        Builders who spec, assemble, and test every system we ship.
      </p>
    </div>
    <div class="grid gap-8 lg:gap-16 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
      {
        items.map(({ name, description, avatar, github_url }) => (
          <div class="text-center text-gray-500 dark:text-gray-400 flex flex-col rounded-lg h-96">
            <div class="flex flex-row justify-center">
              <Image
                src={avatar}
                class="transform hover:scale-105 transition ease-in-out duration-1000 aspect-square hidden md:block"
                alt={`Avatar of ${name}`}
                aspectRatio={1}
                loading="eager"
                width={512}
                height={512}
              />
              <Image
                src={avatar}
                class="transform hover:scale-105 transition ease-in-out duration-1000 aspect-square md:hidden"
                alt={`Avatar of ${name}`}
                aspectRatio={1}
                loading="eager"
                width={256}
                height={256}
              />
            </div>
            <ul class="flex justify-center mt-2 space-x-4">
              <li>
                <a
                  class="text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 focus:outline-hidden focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700 rounded-lg text-sm p-2.5 inline-flex items-center"
                  aria-label="Email"
                  href={github_url}
                >
                  <Icon name="tabler:mail" class="w-5 h-5" />
                </a>
              </li>
            </ul>
            <h3 class="mb-1 text-xl font-bold tracking-tight text-gray-900 dark:text-white">
              {name}
            </h3>
            <p class="mb-4 text-sm font-medium">{description}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
