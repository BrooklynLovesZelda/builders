---
import '@fontsource-variable/inter';
//const colors = require('tailwindcss/colors');

// Nunito
// Dosis
---

<style is:inline is:global>
  :root {
    --builders-font-sans: 'Inter Variable';
    --builders-font-serif: var(--builders-font-sans);
    --builders-font-heading: var(--builders-font-sans);

    --builders-color-primary: rgb(249 115 22);
    --builders-color-secondary: rgb(234 88 12);
    --builders-color-accent: rgb(17 17 17);
  }
</style>
